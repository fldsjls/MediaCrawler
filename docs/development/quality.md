# 质量与验收

> 文档类型：当前验收方法；不在此保存容易过期的测试总数。

## 检查入口

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-workbench.ps1
npm --prefix workers/browser-capture run check
npm --prefix webui run build
npm run docs:check
npm run docs:build
```

按变更运行相关 pytest。环境检查与窄回归验证不同范围，不能用单组测试宣称全项目完成。文档链接通过不证明浏览器性能。

## 双模式与设置验收

- 同一会话切换 RTC / JPEG，页面、登录、目标与任务不变。
- 自动模式随人工交互和采集切换，输入始终遵守暂停屏障。
- 隐藏、重连、切页和反复切换不留重复连接、编码或浏览器。
- RTC 失败降级，画面不阻塞控制、日志、候选和取消。
- 中文、粘贴、滚轮、弹页及缩放后小目标坐标准确。
- 原生捕获校验当前页面的 CaptureHandle；同名页面不能替代身份，导航后须重新确认，普通 HTTP 明确降级。
- 辅助页不进入页面列表、媒体候选或三个 worker 的目标选择；关闭目标后不回退捕获辅助页。
- 协商中隐藏、切页、取消和关闭仍能有界完成；晚到应答不留下孤立连接。
- 设置分类、子项、详情鼠标与键盘可达；保存作用域、返回、主题无状态丢失。
- 桌面、笔记本、手机深浅主题、导航、分隔条、滚动、最大化与错误焦点。

## 性能证据

本次用户目标是人工接管至少 30 帧/秒、自动采集低刷新。必须在接入后的工作台实际通道持续测量，并检查最低分段、输入和生命周期；独立捕获实验达标不等于生产路径达标。

记录日期、代码基线、Windows/浏览器环境、视口、页面样本、模式、测量时长和方法。分别报告实测帧率、输入响应测量、CPU/内存及降级情况。没测量就不填数字；目标值不能写成结果，不承诺固定 60 fps。

本地页面证明传输输入，真实网站仍需登录、加载和输出验收。[RTC 本机测量](../history/2026-09-07-rtc-performance.md)和[后续路线比较](../history/2026-09-07-preview-route-comparison.md)分别记录时点结果。比较同一路径时保留后续回落数据，不能只取最佳一次；后续测试继续按日期存入[历史](../history/index.md)。

采集回归还需队列、暂停、取消、重启、部分成功、重试无重复、数量限制、原脱敏、预览不下载、DASH/分段、广告隔离和 blob 未确认归属。书籍正文与价格无执行器，不能标为通过。
