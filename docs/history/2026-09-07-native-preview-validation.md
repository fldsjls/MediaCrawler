# 2026-09-07 原生预览功能验收

> 文档类型：历史执行快照；功能、生命周期与性能分别记录，不把通过项目数当作帧率结果。
> 路径：工作台当前 `NativeRTCStreams` 与 Chromium 原生标签页捕获；使用独立临时 Profile 和本地网页样本，没有真实平台认证或采集。

> 测量口径勘误：下文最初称为“实际显示”的 40.99、36.72 及 19.97 均来自 `requestVideoFrameCallback` **回调次数**，旧脚本没有保存 `metadata.presentedFrames`。主线程可能漏掉回调，因此这些值保留为回调采样率，不能直接作为合成帧率或物理显示器帧率。也不能反向用发送/解码帧率代替显示。方法依据见[质量与验收](../development/quality.md)。

## 独立生命周期

通过项目的 `BrowserSession.rtc.offer` 接口连接真实 Chromium 接收页面，使用颜色标记核对捕获对象，使用小目标及输入框验证操作。最后一次脚本退出码为 0，以下 7 组通过：

| 检查 | 结果 |
| --- | --- |
| 当前页面捕获 | 解码 1280×720，颜色标记与所选原始页面一致 |
| 辅助页隔离 | 不进入公开列表；以真实辅助页身份构造的媒体请求被公共捕获器拒绝 |
| 同页导航和刷新 | 同一 peer 恢复新页面画面，24 像素小目标点击及前后中文输入通过 |
| 弹出页面 | 选择新页面后重连正确目标，滚轮实际改变页面滚动位置 |
| 隐藏与低刷新 | 最后一个 peer 释放辅助页，JPEG 复用同一 PID，末个订阅退出后停止生产者 |
| 普通 HTTP | 不具 CaptureHandle 的本地模拟 HTTP 域名被明确拒绝实时捕获，不传输无法验证的目标 |
| 会话关闭 | 所属浏览器进程结束 |

原始脚本为 `%TEMP%/mediacrawler-native-lifecycle-audit.py`，最终报告为 `%TEMP%/mediacrawler-native-lifecycle-audit.json`。脚本约 26 秒，记录阶段与捕获状态时间线，不测持续帧率。辅助页媒体检查使用真实 Page 身份和合成 Request，避免“请求被 CSP 拦截所以没有候选”掩盖媒体层缺少过滤的错误。

前两次复测在导航后发现实时 peer 已因尺寸变化关闭；当时源 DOM 已是 1280×720，仍不能仅凭 DOM 推断捕获尺寸正确。修复加入暂停、物理内容区重新对齐和身份复核。最终时间线观察到导航时暂为 1186×720，`needsIdentity` 持续为真；恢复 1280×720 并复核后才恢复画面。同页刷新也通过。

本地测试 HTTP 服务在客户端退出时记录过一次连接重置，这是样本服务的请求关闭日志；应用流程检查通过，最终进程清理完成。

## 六组界面矩阵

隔离 API/Profile 下验证 1440×900、1366×768、390×844，各含浅色和深色，共 6 组通过。检查原生 1280×720 视频、同会话中文输入、实时与低刷新切换、隐藏恢复、导航折叠或抽屉，以及没有页面横向溢出。控制与资源连接分别保持 1 个；实时、低刷、隐藏分别对应 1 个 peer、1 个 JPEG 连接、无画面连接。页面错误列表为空，所属源进程结束。

证据位于 `%TEMP%/mediacrawler-native-ui-six-9seje0nx/` 的桌面报告及 4 张截图，以及 `%TEMP%/mediacrawler-native-ui-six-tfauvr92/` 的手机报告及 2 张截图。首轮手机仅遇到测试定位器同名“关闭导航”按钮歧义，限定主导航后补跑通过。此矩阵不测 FPS；其中导航检查是工作台导航保留会话，同一网站标签页跳转由上面的独立生命周期另验。

## 2026-09-07：目录迁移后的完整工作台测量

用户已确定唯一实时路径为**浏览器原生标签页捕获 → WebRTC**；自动采集继续使用低刷新 JPEG。本轮沿此路径验证，不再把其他捕获方式列为待选实现。

这次测试使用迁移后的 `src/mediacrawler/workbench` 后端模块和 `.build/webui` 的实际前端构建，运行完整工作台页面。API、数据库、Profile 和端口使用独立临时环境；接收窗口为 1440×900、浅色主题，源画面与解码尺寸均为 1280×720，目标页面为本地动态样本。**这不是原 8080 服务的运行验收，也不是仅有捕获发送者的独立技术演示。**

| 指标 | 结果与口径 |
| --- | --- |
| 网页视频回调采样率 | 410 次 / 10.0035 秒，平均 **40.99 次/秒**；未保存合成帧增量 |
| 源捕获速率 | 发送端 `media-source.framesPerSecond` 起止读数为 60 / 59；不是接收端平均帧率 |
| 发送速率 | 末次 `outbound-rtp.framesPerSecond` 为 47；不是该窗口的显示帧率 |
| 编码与尺寸 | VP8 / libvpx，1280×720 |
| 发送端质量限制 | `qualityLimitationReason=none`，未报告 CPU/带宽限制时长；这不是 CPU 利用率测量 |
| 模式与交互 | 点击及同会话中文输入、实时/低刷新、隐藏恢复、工作台导航折叠后连接连续均通过 |
| 连接与清理 | 控制和资源各 1 个连接；实时 1 个 peer，低刷新 1 个 JPEG 连接，隐藏时无画面连接；恢复后 1 个 peer；源进程最终结束 |
| 页面状态 | 1/1 组合通过，无横向溢出，页面错误列表为空，记录的实时降级次数为 0 |

原始报告为 `%TEMP%/mediacrawler-native-ui-six-z1we4v99/result.json`，截图为同目录的 `1440-900-light.png`。上述模式切换与清理均已完成，报告最终 `passed=true`、`source_process_terminated=true`、`errors=[]`。

该 10 秒窗口的平均回调采样率超过 30 次/秒。报告没有合成帧增量、逐秒最低值或长时间稳定性统计，不能据此宣称持续显示目标已完成，也不能把默认配置目标 60 写成实测 60。随后另以实际应用入口复测，见下一节；这两组结果都不构成对旧 8080 低帧率原因的同环境归因。

此轮工作台导航检查指侧栏折叠与会话连续；同一网站标签页导航和刷新见前面的独立生命周期记录。真实平台的页面负载、登录、验证码和采集下载仍需单独验收。

## 2026-09-07：隐藏后台启动实际应用入口

为验证实际启动路径，使用 `mediacrawler.api.main:app` 隐藏后台启动服务，独立端口为 8769，数据与 Profile 位于 TEMP。测试仍打开实际工作台前端，使用带小型动态 canvas、视频元素和输入控件的本地长页面；源 DOM、捕获和接收尺寸均为 1280×720。

| 指标 | 最终复测 |
| --- | --- |
| 网页视频回调平均采样率 | 383 次 / 10.4304 秒，约 **36.72 次/秒** |
| 最低约一秒回调分段 | 20 次 / 1.0014 秒，约 **19.97 次/秒**；不能据此认定合成器只处理了 20 帧 |
| 低刷新 | 配置 2 帧/秒；15 帧 / 10.031 秒，约 **1.50 帧/秒** |
| 五次点击至源页面 HTTP 回执 | 219 / 234 / 250 / 187 / 203 毫秒；不包含新画面返回并显示的延迟 |
| 接收端统计 | `inbound-rtp.framesDropped=0`；该字段不等于网页显示帧率，也不能覆盖最低分段波动 |
| CPU / 内存 | 本轮未记录进程 CPU/RSS，不据此比较整条传输路径的资源消耗 |

20 像素目标点击、中文输入、低刷点击、同会话模式切换、滚轮、自动模式与设置保存均通过。隐藏预览期间仍发现 2 项资源，控制和资源连接各 1 个，页面错误列表为空。完整脚本退出码为 0；主任务随后确认测试会话列表为空，独立服务及其所属进程已清理。

原始报告为 `%TEMP%/mediacrawler-install-check/rtc-native-background-results.json`。回调采样平均值超过 30，但最低分段低于 30；因缺少合成帧增量，**不能据此宣称持续至少 30 帧/秒或锁定 60 已完成，也不能直接认定传输通道跌到 19.97 帧/秒**。前一组约 40.99 与本组约 36.72 分别保留各自样本和启动口径，不挑选较高一次作为稳定性能。

回看原始累计计数：发送编码增加 675 帧 / 11.843541 秒，接收解码增加 639 帧 / 11.303455 秒，分别约 56.99 与 56.53 帧/秒。这两个区间由各端统计时间戳计算，起止不同，也不与 10.4304 秒回调窗口完全重合。此证据促使后续补测合成帧增量，不能用于补造旧报告未记录的逐秒显示结果。

## 相关自动化回归

迁移后的 native、preview、browser、settings 首轮共 47 用例，46 通过、1 失败，真实浏览器用例包含在已通过的 46 项中。失败项以接收端相邻送达时刻判断低刷新生产预算，线程调度迟到可能压缩送达间隔；随后调整为核对实际截图调用时间的累计预算和递增序号，针对该项与画面测试复跑 2 项通过，用时 3.96 秒。这里保留首轮与复跑范围，不将它写成一次 47 项全通过。

## 2026-09-07：迁移后任务与下载补充检查

本轮按既定任务流程审计迁移后的实现，修复以下问题；这些检查不增加实时捕获路线，也不替代真实网站验收。

| 检查范围 | 修复与证据 |
| --- | --- |
| 任务取消与失效会话 | 取消正在等待 HTTP 数据的下载后关闭连接并允许下一任务执行；暂停让当前文件完成并阻止后续分派；资源状态同步取消；已断开的浏览器不再被后备选择逻辑复用 |
| 分片与多视频 | 有序分片保留原始 HTTP 错误以触发过期重解析；失败、取消时清理分片及合并临时文件；知乎、贴吧按独立视频分组，未解析的兄弟视频单独登记失败 |
| 清晰度变化重试 | 明确同一视频的自动失败项可以替换清晰度并复用额度；成功文件、手选版本和其他视频保持独立；原 720p 失败转 1080p 的复现最终只有一个成功文件，无失败槽位残留 |
| 打开新目标 | 空闲及已完成会话复用原浏览器，依次确认接管、导航应答；真实本地页面验证 PID、会话、Cookie、localStorage 保持，控制锁释放前不导航 |
| 配置和任务 | 切换平台后配置区只展示同平台任务操作，原任务事件订阅和任务中心操作仍有效；四个隔离 React 行为样本通过 |
| 包与入口 | 离线锁文件检查、Python/Chromium 依赖、FFmpeg 与 N_m3u8DL 校验、可编辑包入口 `mediacrawler --help` 通过；离线 wheel 中的 `workbench/preview/native_sender.js` 与源码逐字节相同 |

媒体窄回归 26 项通过，包含真实 FFmpeg 的本地分离流及有序分片合并；另 1 项真实 Chromium 的归属、暂停和混合视频样本通过。任务生命周期、媒体身份及重试相关 37 项纯回归通过。不同窄回归范围有重叠，不能相加作为项目测试总数。

打开目标的证据为 `%TEMP%/mediacrawler-open-target-dg9nn3u6/result.json` 和 `%TEMP%/mediacrawler-open-target-live-8mm8gn5p/result.json`。后者使用独立 FastAPI、SQLite、Chromium 与低刷新页面；测试结束清理所属进程，没有操作原 8080 服务。

同一工作区的另一任务正在调整 UI 样式与目录。本节复用行为检查针对实际 React 组件和独立本地应用，不把此前六组界面矩阵视为后续所有样式修改的验收。

## 2026-09-07：冷启动超时与测量复核

补测时发现初始连接偶发失败，不能用此前测试通过覆盖这一现象。前三轮均未取得 60 秒数据：第一次实时已就绪，但可选命令行诊断 API 被 Chromium 拒绝；后两次在就绪前回退低刷新。第三轮的最终阶段曾显示“匹配浏览器视口”，仅是阶段标签，不能据此认定窗口调整实现错误。

随后独立分步诊断记录：创建辅助标签页约 153 毫秒，之后辅助页初始化约 8.45 秒，身份校验相关 CDP 调用约 1.01 秒；捕获结果已经达到 1280×720@60，但 `_capture` 在约第 13 秒被取消。该轮没有进入视口匹配函数；后端期限先到，前端随后中止请求。时序证据为 `%TEMP%/mediacrawler-native-timing-_4jew_b7/timing.jsonl`，不能把不同轮次阶段混作同一次根因。

修复将后端建立连接预算调整为 25 秒，客户端请求为 28 秒、从 ICE 至首帧总预算为 40 秒；客户端 ICE 仍为 5 秒。后端保留超时阶段供查询，关闭及晚到应答仍清理对应 peer。该修复处理有效冷启动被过早中断，不代表视频帧率已经提升。

相关 native 与生命周期回归 24 项通过。真实 React Hook 的虚拟时序共 7 组通过：旧版 ICE 4 秒加应答 12 秒会在 15 秒回退；新版 4+12、4+24 秒成功，请求超时、首帧总期限及隐藏后晚到 peer 释放符合新预算。此验证使用模拟 RTC，不用于评价媒体性能；证据为 `%TEMP%/mc-hook-deadline-results.json`。

修后首次完整 60 秒样本位于 `%TEMP%/mediacrawler-native-60s-ov4mwsls/`。该窗口可能与上述另一 headless React 测试重叠（约 18:15:41–18:17:09），必须保留“额外浏览器负载”的条件，不将其视为独占性能基线或稳定能力上限。

## 2026-09-07：60 秒分层复测

停止本任务团队的其他浏览器及构建后，保持相同本地动态 canvas 样本、1440×900 接收窗口、1280×720 源画面、60 fps 配置和 8 Mbps 上限，重新预热 5 秒并测量 60 个约一秒窗口。两次脚本的样本正文 SHA256 相同：`9e6a15074645ae7013ddc9558b9fdb7896b4182f3fab5198a37e623e393f49fa`。没有修改视频编码或用降低分辨率换取帧率。

独占指本任务团队没有其他测试浏览器或构建同时运行，不表示排除了用户其他程序与系统负载。外层测量操作为 UTC 10:20:45.815108–10:21:47.227770，接收端实际统计区间 60.0024 秒；各端在自身 JavaScript 中采样，起止存在调用时差。平均值按累计计数增量除以对应统计时间计算，最低值和 P10 来自逐秒分段。

| 阶段 | 加权平均 | 最低约一秒 | P10 |
| --- | ---: | ---: | ---: |
| 原生捕获源 | 59.56 fps | 53.56 | 57.83 |
| 编码及发送 | 56.88 fps | 46.51 | 50.18 |
| 接收解码 | 56.87 fps | 45.69 | 50.77 |
| 提交合成：`presentedFrames` 增量 | **49.21 fps** | **39.09** | **42.59** |
| 主线程视频回调次数 | 43.95 次/秒 | 13.47 | 31.74 |

这次提交合成的全部 60 个分段超过 30 fps。源、编码和接收尺寸始终为 1280×720，实际编码为 VP8 / libvpx；CaptureHandle 持续有效，未出现低刷新回退。平均编码耗时 12.62 毫秒/帧，发送端 `qualityLimitationReason` 全为 `none`。接收页面仍有 28 次主线程长任务、合计 2414 毫秒，视频回调低值不能直接等同于传输丢帧。

源浏览器进程组 CPU 平均约 199.56% 单逻辑核（12 逻辑核机器口径约 16.63%），接收组约 64.07% 单核（机器口径约 5.34%）。进程范围来自所属 Chromium 的进程列表，不含全部系统负载；Python API 与测量脚本同进程，不能把其 CPU 当纯 API 开销。两端 DOM 均可见；接收端内部 renderer 后台状态未直接测得，不从 DOM 推断。没有页面脚本错误或框架覆盖层，所有记录的所属 Chromium 进程均已退出。

前一份有重叠条件的 60 秒样本仍保留：提交合成平均 23.05 fps、最低 11.81，编码平均 24.51 fps，曾报告 CPU 限制、平均编码 35.16 毫秒/帧。不能只呈现较高一次，也不能仅凭两次对照断定全部差异由那一项并发测试造成。独占复测已满足此动态样本的一分钟分段目标，因此本轮未继续提高码率或强制其他编码；仍不承诺任意机器、负载与网站均持续 30 或锁定 60。

原始结果、逐秒计数与截图位于 `%TEMP%/mediacrawler-native-60s-qa8epa8r/` 的 `result.json`、`derived.json`、`after.png`。主任务另从原始首尾计数复算得到 49.214698 fps，并核对所有采样尺寸与可见性。样本源页的中文标题存在编码异常，此页用于几何动画及帧率测量，不作为中文显示或输入验收；工作台中文及输入沿用各自交互检查。

## B站 HTTPS 主页预览

修后首次 60 秒样本结束后，另在同一源会话导航到 `https://www.bilibili.com/`。页面标题及正文是正常首页，显示未登录提示，未检测到测试所检查的风险挑战文字；原 peer 保持，原生捕获身份复核有效，接收尺寸 1280×720。证据位于 `%TEMP%/mediacrawler-native-60s-ov4mwsls/result.json` 的 `bilibili_homepage`。

此检查仅证明真实 HTTPS 首页可加载并在同一原生通道预览，没有执行账号动作、登录、评论采集或下载。主页基本静态，记录的约 3.70 次/秒合成提交率不用于动态视频的 30 fps 判定；未验证挑战文字也不能保证不存在其他网站限制。真实视频目标、播放声音与下载完整性仍需另行验收。

## 边界

功能通过组数不能作为帧率证据。60 帧/秒是当前新会话的配置目标；性能以对应运行方式和采样窗口的实际报告为准，不能引用[独立原生捕获实验](2026-09-07-preview-route-comparison.md)的约 55.94 帧/秒替代工作台结果。

HTTPS、本机页面的通道测试不代替真实网站登录、加载、验证码、媒体捕获和下载验收。普通 HTTP 的身份 API 限制仍存在；美石建工当前 HTTP 入口按低刷新边界使用。当前结构见[浏览器架构](../architecture/browser-workspace.md)，后续性能与功能测试遵循[验收方法](../development/quality.md)。
