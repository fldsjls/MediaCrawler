# 2026-09-07 文档重构验收

> 文档类型：历史执行快照
> 环境：Windows 本机；VitePress 1.3.4，Playwright 1.61.0；开发站 `127.0.0.1:8473/MediaCrawler/`。

## 变更范围

文档按入门、使用、架构、开发、运维、历史、ADR 和关于八个分区整理。旧根页面及 workbench 入口提供迁移提示，上游 CLI、存储与署名资料保留。主导航以任务阅读为主，上游推广进入 about；删除 GA 和错误编辑入口。

主题保留 VitePress 搜索、深浅切换和手机导航，统一配色、阅读宽度与页面层级。根 `docs:build` 先执行结构检查，再构建站点。

## 取得的证据

| 检查 | 结果与范围 |
| --- | --- |
| 结构脚本 | 初次通过 76 页、8 分区、24 个旧入口、3 个 ADR；覆盖索引可达性、内部链接和迁移目标 |
| VitePress 构建 | 初次成功，80.18 秒；保留大 chunk 提示，没有死链失败 |
| 桌面界面 | 1440×900，浅色/深色；首页、主题切换、指南导航、旧链接跳转通过 |
| 手机界面 | 390×844，浅色/深色；菜单、指南、旧链接跳转通过 |
| 页面健康 | 四组均有内容，无框架错误覆盖、相关控制台错误或水平溢出 |

测试使用项目已有 Playwright；当前会话没有 Browser 技能。截图和原始 JSON 留在 `%TEMP%/mediacrawler-docs-qa`，临时浏览器已关闭。最初的测试脚本对 SPA 渲染等待及相对链接定位不准确，修正等待和语义定位后四组通过，未把脚本失败当作页面缺陷。

## 边界

这是文档站验收，不是 RTC 帧率、设置保存或真实平台采集验收。文档浏览器检查结束后停止浏览器和构建，避免影响后续性能测量；后续事实修订以最终结构检查和构建为准。
